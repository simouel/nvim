version: "3.3"

services:
  api:
    build: .
    env_file: .env
    command: bash -c "env > /code/.env && uvicorn app.main:app --host 0.0.0.0 --port 80 --reload --env-file .env"

    volumes:
     - ".:/code"
    ports:
      - "8000:80"
      - "5678:5678"
  
